// Example body request structure for get initial information from user

let payload = {
   "token": "string", // Token from web/mobile app
   "client": "string" // web | mobile
}

/* El payload del token provisto por la aplicación web/mobile 
 * debe poseer el objectId como parámetro como se muestra
 * en el ejemplo:
*/
{
    "objectId": "507f1f77bcf86cd99"
}